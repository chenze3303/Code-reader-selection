# HIKROBOT 读码器选型工具 — 使用指南

> 本工具帮助您快速完成读码器选型、竞品对比、配单生成、产品查询和错误排查，无需安装任何软件，打开浏览器即用。

📌 在线地址：https://chenze3303.github.io/Code-reader-selection/

---

## 目录

- [快速开始](#快速开始)
- [首页](#首页)
- [智能选型](#-智能选型)
- [多相机拼接](#-多相机拼接)
- [竞品对标](#-竞品对标)
- [配单表](#-配单表)
- [产品表](#-产品表)
- [状态码查询](#-状态码查询)
- [PDA 选型](#-pda-选型)
- [方案解决](#-方案解决)
- [常见问题](#常见问题)

---

## 快速开始

### 打开方式

1. 双击 `index.html` 文件，浏览器会自动打开
2. 或者直接访问在线地址

> 💡 推荐使用 Chrome、Edge 或 Firefox 浏览器

### 界面概览

打开后你会看到：

- **顶部**：工具名称和主题/语言切换按钮
- **左侧导航栏**（桌面端）：9 个功能入口
- **底部导航栏**（手机端）：3 个入口 +「更多」弹窗

### 切换主题和语言

| 按钮 | 功能 |
|---|---|
| 🌙 / ☀️ | 切换明亮/暗黑模式，你的选择会自动记住 |
| EN / 中 | 切换中英文界面（全站内容均支持双语，品牌名除外） |

---

## ⚡ 智能选型

**用途：** 输入你的应用场景参数，工具自动推荐最合适的读码器型号。

### 操作步骤

**第 1 步：选择码制类型**

在「码制类型」下拉框中选择：

| 选项 | 适用场景 |
|---|---|
| 二维码 (2D) | QR Code、DataMatrix、PDF417 等 |
| 一维码 (1D) | Code128、Code39、EAN-13 等 |

> 💡 不确定？看看你的条码是方的（QR）还是长条的（一维码）

**第 2 步：填写模块尺寸**

模块尺寸 = 条码中最小单元的宽度，单位可选 mm 或 mil。

| 码制 | 典型模块尺寸 |
|---|---|
| QR Code | 0.25 ~ 1.0 mm |
| Code128 | 0.15 ~ 0.5 mm |

> 💡 如果不确定，可以用尺子量一下条码中最小的那个黑块/白块的宽度

**第 3 步：填写工作距离**

读码器镜头到条码表面的距离。

> 💡 如果安装空间还没定，先填一个大概值，比如 100mm、200mm

**第 4 步：填写视野范围**

你希望读码器能看到的区域大小（宽 × 高）。

> 💡 如果条码本身是 30mm × 30mm，视野建议填 50mm × 50mm 以上，留一些余量

**第 5 步：点击「⚡ 开始智能选型」**

等待 1-2 秒，工具会显示：

- 🏆 **最佳推荐型号**：综合评分最高的型号
- 📊 **PPM 值**：像素密度，值越大读码越稳定
- 📐 **预估视野**：该型号在你设定的工作距离下能覆盖的实际视野
- 📏 **工作距离范围**：该型号支持的工作距离

**第 6 步：查看所有满足条件的型号**

点击「📋 查看所有满足条件的型号清单」按钮，可以看到所有符合条件的型号列表：

- 按系列筛选
- 每个型号显示分辨率、接口、防护等级、PPM、工作距离等详细参数
- 第一个型号带有「推荐」标记

### PPM 是什么？

PPM（Pixels Per Module）= 每个最小模块占用的像素数。PPM 越高，读码越稳定。

| 码制 | 优秀 | 良好 | 合格 | 较低 |
|---|---|---|---|---|
| 二维码 (2D) | 4 ~ 8 | 8 ~ 12 | 3 ~ 4 或 12+ | < 3 |
| 一维码 (1D) | 1.4 ~ 2 | 2 ~ 3 | 1 ~ 1.4 或 3+ | < 1 |

### 什么情况下需要多相机拼接？

如果单相机无法满足你的视野或分辨率需求，工具会自动提示「单相机无法满足」，并提供多相机拼接方案。详见[多相机拼接](#-多相机拼接)章节。

---

## 📐 多相机拼接

**用途：** 当单相机视野不够大时，用多个相机组合覆盖更大的区域。

### 什么时候会触发？

在智能选型页面，如果计算结果显示单相机无法满足你的视野需求，页面会自动显示提示卡片，说明无法满足的原因。

### 操作步骤

1. 点击「查看拼接方案」按钮
2. 填写条码尺寸和安全余量
3. 选择条码方向（自动/横向/纵向）
4. 点击「计算拼接方案」

### 结果说明

工具会显示最优拼接方案：

| 信息 | 说明 |
|---|---|
| 推荐型号 | 拼接使用的读码器型号 |
| 相机数量 | 需要几个相机 |
| 排列方式 | 如 2×2、3×2 等 |
| 单机视野 | 每个相机覆盖的区域 |
| 总覆盖范围 | 所有相机组合覆盖的区域 |
| PPM | 拼接后的像素密度 |
| 重叠区域 | 相邻相机之间的重叠宽度 |

还可以查看 SVG 示意图，直观了解相机布局和覆盖范围。

### 查看全部方案

点击「查看全部方案」按钮，可以浏览所有可行的拼接方案。弹窗顶部有一行工具栏：

| 下拉框 | 功能 |
|---|---|
| 系列 | 按产品系列筛选方案（全部 / 指定系列） |
| 排序 | 按相机数量（↑ 升序 / ↓ 降序）或相机分辨率（↑ 升序 / ↓ 降序）排序，可选「默认」恢复推荐顺序 |

点击任意方案即可切换到该方案查看详情。

---

## 🔬 竞品对标

**用途：** 对比友商读码器和海康读码器的对应关系，方便竞品替换。

### 操作步骤

1. 在搜索框输入友商型号或海康型号（如 `SR-1000`、`DM70`）
2. 或者在品牌下拉框中选择品牌（Cognex、Keyence、Datalogic 等）
3. 点击「📂 展开所有」查看全部对标数据

### 卡片内容说明

每张卡片包含：

| 区域 | 颜色 | 内容 |
|---|---|---|
| 📌 友商核心特点 | 蓝色标签 | 友商型号的参数和特性 |
| ✨ 海康竞争优势 | 绿色标签 | 海康对应型号的优势 |
| 🎯 推荐型号 | — | 海康推荐的对应型号 |

### 支持搜索的关键词

- 友商型号：`SR-1000`、`DM70`、`Matrix 220`
- 海康型号：`ID3013PM`、`ID2023XM`
- 品牌名：`Cognex`、`Keyence`、`思谋`

> 💡 搜索自动忽略 `MV-` 前缀和大小写，输入 `id3013` 也能搜到

---

## 📋 配单表

**用途：** 根据选定的读码器型号，自动生成完整的 BOM（物料清单），包括主机、标配配件和选装配件。

### 操作步骤

**第 1 步：选择产品大类**

从下拉框选择产品大类（如 ID800系列、ID2013EM系列等）。

**第 2 步：选择产品系列**

选好大类后，系列下拉框会自动填充可选项。

**第 3 步：选择具体型号**

选好系列后，型号下拉框会自动填充可选项。

**第 4 步：自动生成配单**

选定型号后，配单会自动生成，包括：
- 🔵 **主机**：读码器本身
- 🔵 **标配**：出厂自带的配件（线缆等）
- 🟠 **选配**：需要额外购买的配件

### 快速搜索

**用途：** 想直接跳到某个型号，或想知道某件配件适用于哪些产品时使用。

- 在「快速搜索」框输入**型号名称、物料代码或配件物料号**
- 搜到**产品型号**：点击直接选中并生成配单
- 搜到**配件**：会显示该配件适配的**产品系列标签**（如 `ID3000 3`，数字表示该系列下适配的型号数）
  - 点击某个**系列标签**：自动跳转选中该系列并生成配单，同时把你搜索的配件也加入配单
  - 点击配件条目其余区域：直接把该配件加入配单

### 选装配件

如果该型号有选装配件，会显示配件分类卡片（如线缆、电源、安装板等）。

1. 点击某个分类卡片，弹出该分类下的所有配件
2. 点击配件行即可勾选/取消
3. 线缆类配件支持按**长度**（2m、3m、5m 等）和**材质**（普通、高柔、超柔）筛选
4. 勾选后配单会自动更新

> 💡 **配件图片**：配件弹窗和 BOM 表格会自动显示配件缩略图（99 个配件、33 张图），方便识别

> 💡 **电源联动**：选中电源适配器时，对应的电源线缆会自动勾选，反之亦然

> 💡 **标配替换**：当选中某个分类的选配件时，该分类的标配配件会自动移除

### 其他功能

| 按钮 | 功能 |
|---|---|
| ⬇ 导出 CSV | 将配单导出为 CSV 文件，可直接用 Excel 打开 |
| 重置 | 清除所有选装配件，恢复默认配单（被选配替换掉的标配会自动恢复） |
| 📥 资料下载 | 点击跳转到海康官网下载该型号的技术资料 |

### 配单会丢失吗？

**不会。** 你的选型状态（选中的大类、系列、型号、配件勾选）会自动保存到浏览器中。即使刷新页面或关闭浏览器重新打开，之前的配单也会自动恢复。

> ⚠️ 注意：清除浏览器数据会导致配单丢失。重要配单建议导出 CSV 保存。

---

## 🔄 产品表

**用途：** 查询海康读码器基线型号和经销型号的对应关系及物料代码。

### 什么是基线和经销？

| 类型 | 说明 |
|---|---|
| 基线型号 | 直销渠道的物料（直接从海康购买） |
| 经销型号 | 渠道代理的物料（通过经销商购买） |

> 💡 同一个产品有两种型号代码，对应不同的销售渠道

### 操作步骤

1. 在搜索框输入型号名称或物料代码（如 `ID803`、`313201715`）
2. 或者在系列下拉框中选择特定系列
3. 点击系列名称可以展开/收起该系列的型号列表
4. 点击「📂 全部展开」或「📁 全部收起」控制显示

### 资料下载

每行右侧有两个 📥 按钮：
- **基线资料**：跳转到海康官网下载基线型号的技术资料
- **经销资料**：跳转到海康官网下载经销型号的技术资料

> 💡 如果按钮显示「—」，表示该型号暂无下载链接

### 命名规则

点击「📖 命名规则」按钮可查看海康 MV-ID 读码器型号的完整命名规则：
- 型号结构分解（品牌/品类、系列、类型、焦距、调焦、光源、变体、镜头）
- 点击任意结构块可查看该字段的详细说明与编码对照表
- 底部附 8 个真实型号的解析示例

### 隐藏功能

连续点击 4 次「产品表」导航标签，可以显示/隐藏物料代码列。

---

## 🔍 状态码查询

**用途：** 查询海康读码器 SDK 返回的错误码含义和解决方法。

### 操作步骤

1. 在搜索框输入状态码名称或值（如 `MV_CODEREADER_E_HANDLE` 或 `0x80020000`）
2. 或者在分类下拉框中选择错误类型
3. 点击表格行可以复制状态码名称到剪贴板

### 分类说明

| 分类 | 数量 | 说明 |
|---|---|---|
| 正确码 | 1 | 成功状态 |
| 通用错误码 | 94 | 句柄、参数、资源、版本等通用错误 |
| IDMVS自定义 | 25 | IDMVS 软件特有错误 |
| 读码器控制 | 14 | 读码器对象操作错误 |
| GenICam相关 | 10 | GenICam 协议相关 |
| 网口读码器 | 8 | 以太网通信相关 |
| U口读码器 | 7 | USB 通信相关 |
| 升级组件 | 5 | 固件升级操作 |
| 底层组件 | 83 | 底层 SDK 各模块错误 |
| 网络组件 | 10 | Socket 网络通信 |

### 解决方法

162 条状态码附带**解决方法**列，直接告诉你怎么处理这个错误。

> 💡 搜索时会同时匹配解决方法内容，比如搜「重启」可以找到所有建议重启的状态码

---

## 📱 PDA 选型

**用途：** 对比 IDP 系列智能移动终端（PDA）型号参数，多条件筛选后快速锁定合适机型。

### 操作步骤

**第 1 步：进入 PDA 选型**

- 桌面端：点击首页「📱 PDA 选型」卡片，或左侧导航栏「PDA 选型」
- 手机端：点击底部「更多」→「PDA 选型」

**第 2 步：按条件筛选**

页面顶部有 8 个下拉筛选框：

| 筛选条件 | 可选值 |
|---|---|
| 系列 | 8 大系列 |
| 防护等级 | IP65 / IP67 / IP68 |
| NFC | 支持 / 不支持 |
| 操作系统 | Android 10 / 11 / 13 / 14 |
| 屏幕尺寸 | 4.0 / 5.0 / 5.5 / 6.0 / 6.2 英寸 |
| 处理器 | 2.0 / 2.2 / 2.6 GHz |
| OCR | 支持 / 不支持 |
| 电池容量 | 4500 / 4850 / 4900 / 5000 / 5200 mAh |

选择条件后表格自动过滤，顶部显示当前命中的型号数量。多个条件可叠加组合使用。

**第 3 步：对比参数**

- 表格共 39 行参数（条码类别、OCR、处理器、内存、操作系统、屏幕、NFC、防护等级、电池等）
- 左侧参数名列固定吸附，横向滚动时始终可见
- 型号较多时表格自动横向滚动（每列保持 150px 可读宽度），型号较少时列宽自动放大填满视口

> 💡 变体型号（如 `MV-IDP3204/64G`）会继承同系列基准型号的参数，仅差异项单独标注，对比更完整

---

## 📖 方案解决

**用途：** 快速访问常用资源和工具。

### 可用资源

| 卡片 | 说明 |
|---|---|
| 🤖 小V智能助手 | 在线智能问答，快速解决产品使用与选型问题 |
| 📄 方案解决文档 | 应用方案、接线指南、调试教程等 |
| 🔧 维修状态查询 | 查询产品维修进度与售后状态 |

> 💡 所有链接都在新标签页中打开，不会离开当前页面

---

## 🤖 机器人助手（小V）

页面右下角有一个可爱的机器人吉祥物，它是小V智能助手的快捷入口。

- **点击**：跳转到海康机器视觉 v-club 智能助手
- **悬停**：机器人会做出有趣的动画反应
- **气泡提示**：首次打开页面时会显示 15 秒的欢迎语

> 💡 手机端不显示机器人助手，可以通过「方案解决」页面访问智能助手

---

## 常见问题

### Q：页面打不开 / 显示空白？

**A：**
- 确保用 Chrome、Edge 或 Firefox 浏览器
- 如果是本地打开（双击 index.html），确保所有文件都在同一目录下
- 如果是在线访问，尝试清除浏览器缓存后刷新

### Q：智能选型没有推荐结果？

**A：**
- 检查参数是否合理（工作距离不要填太大或太小）
- 尝试减小视野范围或增大工作距离
- 如果确实没有匹配的型号，可能是当前产品库未覆盖你的场景，可以联系海康技术支持

### Q：配单表选不到我要的型号？

**A：**
- 先确认型号在哪个大类下（如 ID800、ID2013EM 等）
- 如果产品库中没有该型号，可能是新品尚未录入

### Q：产品表的下载按钮显示「—」？

**A：**
- 部分型号的资料下载链接尚未收录
- 可以直接访问海康官网搜索该型号下载资料

### Q：配单刷新后消失了？

**A：**
- 正常情况下配单会自动保存，刷新后会恢复
- 如果消失了，可能是清除了浏览器数据，或者使用了隐私模式
- 建议重要配单及时导出 CSV 保存

### Q：暗黑模式下某些文字看不清？

**A：**
- 这可能是已知问题，请反馈给工具维护者
- 临时切换到明亮模式查看

### Q：手机上怎么用？

**A：**
- 手机端底部有 3 个导航标签 +「更多」按钮
- 点击「更多」可以访问其他功能（含 PDA 选型、配单表、产品表等）
- 首页功能卡片在手机上以 2 列网格显示，超长描述自动截断为 2 行
- 配单表在手机上可能需要左右滑动查看完整表格

---

## 快捷键和隐藏功能

| 操作 | 效果 |
|---|---|
| 连续点击左上角 Logo 3 次 | 跳转到数据库编辑器 |
| 连续点击「产品表」标签 4 次 | 显示/隐藏物料代码列 |
| 点击状态码表格行 | 复制状态码名称到剪贴板 |
| `Ctrl + S`（数据库编辑器中） | 快速保存 |
| `Esc`（数据库编辑器图片弹窗中） | 关闭图片选择弹窗 |

### 数据库编辑器图片管理

在数据库编辑器的**配单 tab** 中，每个配件行新增"图片"列：

- **已配图**：显示 44×44 缩略图，点击可更换图片，✕ 按钮清除图片
- **未配图**：显示"＋ 图"按钮，点击打开图片选择弹窗
- **图片选择弹窗**：浏览全部 33 张 webp 图片，支持搜索过滤，点击即应用
- **本地上传**：点击"本地上传"选择本地图片，自动压缩为 webp 并保存
- **导出 acc_imgs.js**：header 按钮，一键导出配件图片映射文件

---

## 反馈与建议

如有问题或建议，请联系工具维护者。

---

*最后更新：2026-08-18 | V4.1*
